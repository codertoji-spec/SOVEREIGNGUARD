$kind: http-example
name: Template deleted.
request:
  url: "{{baseUrl}}/v1/signatures/template/:templateId/delete"
  method: DELETE
  headers:
    Accept: application/json
  pathVariables:
    - key: templateId
      value: string
      description: (Required) The ID of the template that should be deleted.
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "result": {
          "statusCode": 200,
          "message": "OK",
          "data": {
            "template": {
              "id": "string",
              "message": "string"
            }
          }
        },
        "origin": "string",
        "dateTime": "string"
      }
order: 1000
