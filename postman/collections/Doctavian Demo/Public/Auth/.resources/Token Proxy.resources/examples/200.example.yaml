$kind: http-example
request:
  url: "{{baseUrl}}/public/v1/auth/:provider/token"
  method: POST
  pathVariables:
    - key: provider
      value: ""
      description: "(Required) "
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
